# Task Board - 任务看板

一个基于 React + TypeScript + Vite 的任务管理看板应用。

## 解决方案与设计决策概述

### 架构设计

本项目采用现代化的前端架构，以 React 19 为核心，结合 TypeScript 提供类型安全。选择 Vite 作为构建工具，相比传统 CRA 提供更快的开发体验和更优的构建性能。

### 状态管理策略

使用 Zustand 替代 Redux 或 Context API，原因如下：

- **轻量级**：无需 Provider 包裹，代码更简洁
- **TypeScript 友好**：天然支持类型推断
- **持久化支持**：通过中间件轻松实现状态持久化
- **性能优秀**：仅订阅需要的字段，避免不必要的重渲染

### 拖拽实现方案

选择 `@dnd-kit` 而非 `react-beautiful-dnd`，因为：

- **现代化**：基于 React 18+ 的并发特性设计
- **可定制性**：提供更灵活的 API，支持复杂的拖拽场景
- **无障碍支持**：内置键盘和屏幕阅读器支持
- **性能**：使用虚拟化策略处理大量数据

### 权限控制设计

采用前端 + 后端双重验证：

- **前端**：根据 `userId` 判断操作权限，隐藏或禁用不可操作元素
- **后端**：Supabase RLS 策略确保数据安全
- **游客模式**：通过 `is_anonymous` 字段区分，提供只读体验

### 样式方案

使用 SCSS + CSS 变量：

- **变量管理**：集中管理颜色、间距、阴影等设计令牌
- **模块化**：每个组件独立样式文件，避免冲突
- **响应式**：CSS Grid + Flexbox 实现自适应布局

### 数据流设计

- **乐观更新**：拖拽操作先更新 UI，再调用 API，失败时回滚
- **本地缓存**：Zustand 持久化减少重复请求

## 本地运行说明

### 环境要求

- Node.js 18+
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

### 构建生产版本

```bash
npm run build
```

## 高级功能

### 1. 标签系统

- 支持多标签输入，以逗号分隔存储
- 任务卡片和详情页显示标签
- 支持按标签筛选看板
- 可添加和删除标签

### 2. 截止日期指示器

- 自动检测逾期任务：截止日期已过且状态不是已完成
- 逾期任务卡片显示红色边框、阴影和 `Overdue` 标识
- 详情页中高亮显示逾期状态
- 日期格式化显示，便于阅读

### 3. 搜索和筛选

- **搜索栏**：按任务标题实时搜索
- **状态筛选**：按任务状态（待办 / 进行中 / 审核中 / 已完成）筛选
- **优先级筛选**：按优先级（低 / 中 / 高）筛选
- **标签筛选**：按标签筛选任务
- 筛选条件可组合使用，支持一键重置

### 4. 看板摘要 / 统计

- 显示任务总数、各状态任务数量
- 显示逾期任务数量
- 统计区域与搜索筛选集成在同一行
- 实时更新，数据一目了然

## 技术栈

- **框架**：React 19 + TypeScript
- **构建工具**：Vite
- **状态管理**：Zustand
- **路由**：React Router v7
- **拖拽**：`@dnd-kit`
- **样式**：SCSS
- **后端**：Supabase

## 改进方向

### 未完成的高级功能（1、2、3）

#### 1. 团队成员和任务分配

- 创建团队成员表，支持添加成员（姓名 + 头像 / 颜色）
- 在看板 UI 中显示团队成员列表
- 支持为一个任务分配一个或多个团队成员
- 在任务卡片上显示被分配者的头像

#### 2. 任务评论

- 在 Supabase 中创建单独的 `comments` 表
- 任务详情面板中添加评论输入框
- 按时间顺序显示评论并带有时间戳
- 支持回复和删除评论

#### 3. 任务活动日志

- 创建 `activity_logs` 表记录任务变更历史
- 记录状态变更、编辑、分配等操作
- 在任务详情视图中显示活动时间线
- 例如：`2小时前 从 待办 移动到 进行中`

### 其他改进方向

#### 4. 实时同步

- 使用 Supabase Realtime 实现多用户实时同步
- 一个用户修改任务，其他用户立即看到变化

#### 5. 任务排序

- 添加自定义排序字段
- 支持拖拽排序并持久化

#### 6. 附件上传

- 支持上传图片和文件
- 使用 Supabase Storage

#### 7. 性能优化

- 虚拟滚动处理大量任务
- 分页加载任务列表

#### 8. 移动端适配

- 优化移动端触摸交互
- 添加 PWA 支持

#### 9. 主题切换

- 添加主题切换按钮
- 支持跟随系统主题
